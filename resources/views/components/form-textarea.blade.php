<textarea id="{{ $id }}" name="{{ $id }}" type="text" placeholder="{{ $placeholder }}..." rows="{{ $rows }}}" required
          class="bg-background border rounded border-black outline-0 placeholder-black
                                         p-1.5 resize-none">{{ $slot }}</textarea>
